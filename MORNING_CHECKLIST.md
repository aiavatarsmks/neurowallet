# MORNING_CHECKLIST — что сделать утром (2026-07-03)

> Порядок важен: сначала SQL-миграции (по номерам), потом smoke.
> Код написан так, что до миграций фичи деградируют тихо — ничего не сломано,
> просто таблицы не наполняются.

## 1. SQL-миграции (Supabase → SQL Editor, вставить целиком и Run)

- [ ] `supabase/migrations/0003_tx_drafts.sql` — драфты + симуляция (задача 1.2, уже принято вчера)
- [ ] `supabase/migrations/0004_risk_events.sql` — risk engine (задача 1.3)
- [ ] `supabase/migrations/0005_contacts.sql` — адресная книга (задача 1.4)
- [ ] `supabase/migrations/0006_payment_requests.sql` — платёжные ссылки (задача 1.5)
- [ ] `supabase/migrations/0007_devices.sql` — устройства security center (задача 1.6)

## 2. Smoke в Telegram Mini App (после миграций)

- [ ] Send → адрес/сумма → review-карточка: живая комиссия + «Останется после отправки» (1.2)
- [ ] Review: жёлтый shield «первый перевод на этот адрес» на новом адресе (1.3)
- [ ] Ввести сумму больше баланса → красный блок, кнопка неактивна (1.2)

## 3. Проверка данных (Supabase → Table Editor)

- [ ] `tx_drafts` — появляются строки после каждого review
- [ ] `risk_events` — строки с level warning на новые адреса
- [ ] `analytics_events` — события send_review_shown / risk_flagged
- [ ] `contacts` — контакт появляется после «Сохранить и продолжить» в Send (1.4)
- [ ] Receive → «Запросить оплату ссылкой» → создать → открыть ссылку в обычном браузере: карточка с QR/суммой; протухание проверяется автоматически (1.5)
- [ ] Профиль → «Центр безопасности»: PIN-статус, устройство (после перезахода), лента событий, кнопка «Выйти на других устройствах» (1.6)
- [ ] История → ✨ на транзакции → объяснение Нейры из фактов (1.7)
- [ ] Send review → «✨ Спросить Нейру об этом переводе» (1.7)
- [ ] Чат Нейры → чип «✨ Итоги 24ч» → recap по балансам (1.7)
- [ ] Выйти в демо-режим → на главной карточка «Нейра: попробуй три вещи» → выполнить все три → появляется CTA «Создать настоящий кошелёк» (1.8)
- [ ] В analytics_events — demo_task_completed ×3, demo_funnel_completed, при клике CTA — demo_convert_clicked (1.8)

## 4. Вопросы, требующие твоего решения

- [ ] **NeuroID privacy (D-1.4-3):** нужен ли toggle «показывать меня в
      директории»? Сейчас любой authenticated может отрезолвить любой NeuroID
      (by design справочника). Если нужен — скажи дефолт (opt-in/opt-out),
      сделаю колонку is_public + фильтр в resolve.

---
*Файл пополняется по ходу ночи — блокеры и шаги задач 1.4–1.8 будут дописаны ниже.*

---

# 🌙 Ночь #2 (2026-07-08) — что сделано и что нужно от тебя

## ✅ Сделано и в проде (все тесты зелёные: 145; lint/tsc чисто)

### Задача 1 — Demo-режим (баги найдены прогоном в браузере на проде)
- [x] **Единый источник демо-данных** (`lib/demo-data.ts`) — устранил расхождение
      балансов между экранами (TON/TRX/USDT_TRC были 0 в портфеле, но не-ноль
      в send; тоталы home≠портфель). Коммит 0162f32.
- [x] **Клавиатура сумм** — type=number → text+inputMode=decimal + sanitizeAmountInput
      (все 3 поля: крипто-send, fiat-send, receive). Коммит c939d9b.
- [x] **Мёртвая ссылка на эксплорер в демо** (fake demo-хэш → 404) — скрыта в демо;
      подзаголовок «отправлено в сеть» → «Демо: в реальную сеть ничего не отправлено».
      Коммит 2d9978d.
- [x] **USDT-нейминг** на чипах send выровнен (ERC-20/TRC-20). Коммит 0162f32.
- **Проверь глазами:** демо → Активы (все чейны не-ноль) → крипто-send (клавиатура
      с точкой на телефоне) → завершение (нет битой ссылки).

### Задача 2.4 — Notification engine v1 (in-app inbox)
- [x] **Миграция 0008_notifications ПРИМЕНЕНА на проде** (SQL Editor, main PRODUCTION).
      Проверено: `table_exists=notifications, rls_on=true, policy_count=1`.
- [x] Backend: `writeNotification` (service role, текст композитит СЕРВЕР по
      allowlist-шаблонам — клиент не шлёт текст, нет сумм/адресов), `/api/notifications/emit`
      (authed, rate-limited, allowlist kinds, валидация coin), `/api/notifications/read`.
- [x] `NotificationsInbox` в профиле (RLS SELECT own; рендерит null если пусто/демо/ошибка).
- [x] Эмит `tx_sent` при успешной РЕАЛЬНОЙ отправке (fire-and-forget, не в демо).
- **Проверь:** реальный кошелёк → отправь тестовую сумму → в профиле появится
      «Перевод отправлен».

### Decision docs (задача 6)
- [x] `DECISION_2.1.md` — embedded-wallet провайдеры (Web3Auth vs Turnkey/…; TON+TRON+SOL,
      custody, Telegram WebView, passkey, цены). Вывод: Web3Auth.
- [x] `DECISION_2.2.md` — swap-роутеры (1inch/LI.FI/0x). Вывод: 1inch для v1
      (SOL+TON+EVM), TRX/BTC вне scope v1; BTC-своп только через cross-chain (LI.FI).

## ⏸️ НЕ реализовано — блокеры/решения нужны от тебя

### Задача 2.4 (остаток) — доставка и настройки
- [ ] **Telegram-доставка уведомлений** — НЕ включал: рассылка тестерам запрещена
      без твоего разрешения. Нужно: (а) решение «шлём ли в Telegram», (б) opt-in.
      Инфра готова (`tg-notify` существует) — включается отдельным emitter'ом.
- [ ] **Preference center + quiet hours + дедуп по каналам** — нужна UI-страница
      настроек и таблица `notification_prefs`. Решение: дефолты (какие типы вкл/выкл,
      часы тишины).
- [ ] **tx_confirmed / tx_failed / first_receive** — требуют фонового поллинга
      статуса on-chain и детекции входящих (крон/воркер). Сейчас эмитится только
      `tx_sent` (клиентское событие). Нужен воркер (Vercel Cron?) — решение по инфре.

### Задача 2.5 — Portfolio home v2
- [ ] **Спам-фильтр / verified tokens — ПРЕЖДЕВРЕМЕННО.** Приложение показывает
      8 фиксированных ассетов (нет token discovery произвольных токенов) → фильтровать
      нечего. Актуально станет после произвольного token discovery. Оставил как есть.
- [x] **<1.5с из кэша — уже есть** (кэш балансов, MARKET_REFRESH_MS).
- [ ] **balance_snapshots** (для недельной динамики / 2.7) — не создавал: это чистая
      инфра без потребителя сегодня. Готов сделать миграцию + write-path по твоему сигналу.
- **Решение:** нужен ли token discovery (тогда спам-фильтр осмыслен) и снапшоты сейчас?

### Задача 2.6 — Contact discovery (ENS/TON resolvers)
- [ ] **НЕ трогал live send-путь.** Резолв имени `.eth`/`.ton` определяет КУДА уйдут
      реальные деньги — ошибка резолва = потеря средств. Не хотел вживлять в send
      автономно ночью без твоей проверки на реальном резолве.
- **Готовый план (утром, ~2-3ч):** `lib/name-resolvers.ts` — `resolveEns` (ethers +
      cloudflare-eth, в CSP), `resolveTonDns` (tonapi.io `/v2/dns/{domain}/resolve`,
      в CSP); детект `.eth`/`.ton` в поле адреса CryptoSendScreen; показать
      резолвнутый адрес ЯВНО перед отправкой (anti-poisoning уже есть). Telegram
      contacts — только если не требует новых bot-разрешений (иначе отдельно).
- **Решение:** ок вживлять резолв в send? (это про направление реальных средств).

### Задача 2.7 — Weekly AI recap v1
- [ ] **Блокер: нет снапшотов (2.5)** → истинной недельной динамики нет (только 24ч,
      как в существующем recap 1.7). Inbox для карточки готов (2.4).
- [ ] **Telegram-доставка recap — запрещена без разрешения** (тестерам не писать).
- **Готовый план:** после снапшотов — крон собирает недельный recap из фактов
      балансов → `weekly_recap` в inbox (+ Telegram по решению).

## Env / инфра, которые нужны (секреты не вывожу — только имена)
- [ ] `UPSTASH_REDIS_REST_URL` / `UPSTASH_REDIS_REST_TOKEN` — если ещё не в Vercel:
      без них rate-limit и demo-бюджет деградируют в per-lambda (мягче). Проверь в Vercel env.
- [ ] Решение по Vercel Cron (для tx-confirmed поллинга и weekly recap) — платный ли план.

## ⚠️ Контекст (см. NIGHT_DECISIONS D-N2-0)
Задачи 2.x начаты по твоему указанию, хотя **Gate Фазы 1 (usability-прогон) не пройден**.
Инварианты безопасности CLAUDE.md соблюдены строго. Реальную интеграцию Web3Auth в
онбординг НЕ начинал (ждёт Gate Фазы 1 + твоего PoC).
