generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Define your models here when you begin persisting data. For the MVP
// skeleton we operate entirely on mock data.